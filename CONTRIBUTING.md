# دليل المساهمة في Dawen AI

نشكرك على اهتمامك بالمساهمة في مشروع Dawen AI! هدفنا هو بناء أداة قوية ومفتوحة المصدر تخدم المجتمع العربي.

## كيف تساهم؟

نرحب بجميع أنواع المساهمات، سواء كانت:
*   🐛 **إبلاغ عن أخطاء (Issues)**: إذا وجدت مشكلة، افتح Issue واشرح التفاصيل.
*   💡 **اقتراح ميزات جديدة**: هل لديك فكرة رائعة؟ شاركها معنا!
*   💻 **كتابة كود (Pull Requests)**: ساعدنا في تحسين الكود أو adding features.

## إعداد بيئة التطوير

للبدء في تطوير المشروع محلياً:

1.  **Fork** للمشروع على GitHub.
2.  **Clone** للنسخة الخاصة بك:
    ```bash
    git clone https://github.com/YOUR_USERNAME/dawen_ai.git
    ```
3.  قم بإنشاء بيئة افتراضية (Virtual Environment) لضمان عدم تداخل المكتبات:
    ```bash
    python -m venv .venv
    # Windows
    .venv\Scripts\activate
    # Mac/Linux
    source .venv/bin/activate
    ```
4.  ثبت المكتبات:
    ```bash
    cd ai-seo-writer
    pip install -r requirements.txt
    ```

## قواعد الكود (Code Style)

*   نحاول الحفاظ على الكود **بسيطاً ومقروءاً**.
*   استخدم التعليقات (Comments) لشرح المنطق المعقد.
*   التزم بهيكلية المجلدات الحالية (Modular Structure).
*   إذا قمت بإضافة مكتبة جديدة، لا تنسَ تحديث ملف `requirements.txt`.

## إضافة مزود ذكاء اصطناعي جديد

إذا أردت إضافة دعم لـ OpenAI مثلاً:
1.  اذهب إلى `app/ai/provider.py`.
2.  قم بإنشاء Class جديد يرث من `BaseAIProvider`.
3.  أضف المنطق الخاص بـ OpenAI بداخله.
4.  قم بتحديث دالة `get_provider` لتدعم الاختيار بين المزودين.

شكراً لمساهمتك في إثراء المحتوى العربي المفتوح المصدر! 🚀
